import { render } from '@testing-library/react'
import App from '../App'

describe('', () => {
  test('renders text Template ReactJs - Quantica', () => {
    render(<App />)
    expect(1).toEqual(1)
  })
})
